query getBoxes($start: Int, $limit: Int) {
  boxes(sort: "id:desc", start: $start, limit: $limit) {
    id
    tags {
      id
    }
    countmin
    countmax
    price {
      id
      additional
      discount
      overall
    }
    info {
      id
      name
      description
      image {
        id
        url
      }
    }
    dimensions {
      id
      width
      breadth
      height
      weight
    }
  }
}
