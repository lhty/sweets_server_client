overwrite: true
schema: http://localhost:3000/graphql
generates:
  ./src/graphql/queryTypes.d.ts:
    plugins:
      - add: "/* tslint:disable */"
      - typescript
      - typescript-operations
      # - typescript-react-apollo
      - typescript-graphql-files-modules
plugins:
  - "introspection"
