query getBoxes($start: Int, $limit: Int) {
  items(sort: "id:desc", start: $start, limit: $limit) {
    id
    tags {
      id
    }
    letter
    materials {
      id
      info {
        name
      }
    }
    price {
      additional
      discount
      overall
    }
    info {
      name
      description
    }
    dimensions {
      width
      breadth
      height
      weight
    }
  }
}
